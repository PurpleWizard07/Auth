{
  "name": "auth-starter-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Email + password authentication starter — React + FastAPI + PostgreSQL, JWT strategy",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
